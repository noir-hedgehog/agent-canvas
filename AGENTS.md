# AgentCanvas 开发约定

产品是人与 Agent 共用持久化对象的画布。保留用户布局和数据，不把界面缺陷改写成业务卡片内容。公开示例、测试和发布资产使用合成数据；本地 `.agentcanvas/`、凭据及工作区私有文件不得发布。

## 功能完成标准

新增或改变功能时，同步检查并提交：

1. 共享模型、版本冲突、事务和恢复语义，以及网页交互。
2. MCP 的读取、写入、参数 schema、工具发现与 instructions。用户可持久化创建/修改的内容应可经 MCP 操作；可复用现有工具，但要文档化准确调用路径。只暴露网页按钮不算完成。
3. `skills/agent-canvas/SKILL.md` 中相关规范，`references/mcp-reference.md`、`references/capabilities.json` 中能力与限制，以及需要调整的图表 JSON 范例。明确当前能力，不把计划写成已实现。
4. 至少验证与变更相关的真实协议行为；交互变更做对应浏览器验证。MCP 新增/删除工具会触发指南覆盖检查；范例必须能在隔离项目通过真实 MCP 执行。
5. 本地应用与 Pages 的适用差异、发行包内指南和安装脚本。不要改写已发布的旧版本归档；实际接入测试和协议测试分开陈述。

`read_usage_guide` 从仓库内 Skill 文件直接读取，避免维护第二份复制说明。`npm run skill:install` 把同一目录链接到当前用户的技能目录，不覆盖别的 Skill。用户目录存在不同版本时先核对来源。

不得为验证默认修改用户真实画布；使用临时数据库或公开示例。服务启动/重启按 local-ops 已登记配置进行。
